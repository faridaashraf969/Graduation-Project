@model Cart

<h2>Your Shopping Cart</h2>

@if (Model.CartItems.Count == 0)
{
    <p>Your cart is empty.</p>
}
else
{
    <table>
        <thead>
            <tr>
                <th>Course</th>
                <th>Instructor UserName</th>
                <th>Quantity</th>
                <th>Price</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.CartItems)
            {
                <tr>
                    <td>@item.Course.Topic</td>
                    <td>@item.Course.Instructor.UserName</td>
                    <td>@item.Quantity</td>
                    <td>@item.Price</td>
                    <td>@(item.Quantity * item.Price)</td>
                </tr>
            }
        </tbody>
    </table>

    <p>Total Amount: @Model.CartItems.Sum(ci => ci.Quantity * ci.Price)</p>

    <form asp-action="ClearCart" method="post">
        <button type="submit">Clear Cart</button>
    </form>
    <form asp-controller="order" asp-action="CourseCheckout" method="post">
        <button type="submit">Checkout</button>
    </form>
}

